name: 'secrets-inherit'
on: workflow_dispatch

permissions:
  contents: read

jobs:
  dependency-review:
    runs-on: ubuntu-latest
    steps:
      - name: "Echo Test"
        run: echo "Test"
      - name: 'Running Reusable Workflow'
        uses: test-organization-wengonn-v2/reusable-workflow/echo-secrets.yml@main
        with:
          secrets: inherit
